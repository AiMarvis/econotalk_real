<div class="container mx-auto px-4 py-8 max-w-2xl">
  <!-- Header Section -->
  <div class="flex items-center space-x-4 mb-8">
    <%= link_to @content, class: "inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 h-9 w-9", style: "border: 1px solid #E2E8F0; background-color: transparent; color: #1A202C; --tw-ring-color: #0052CC;" do %>
      <%= icon('arrow-left', class: 'h-4 w-4') %>
    <% end %>
    
    <div class="space-y-1">
      <h1 class="text-2xl font-bold tracking-tight" style="color: #1A202C;">
        콘텐츠 수정
      </h1>
      <p class="text-sm" style="color: #718096;">
        <%= @content.title %>
      </p>
    </div>
  </div>

  <!-- Form Section -->
  <div class="rounded-lg border p-6" style="border-color: #E2E8F0; background-color: #FFFFFF;">
    <%= render 'form', content: @content %>
  </div>

  <!-- Actions Section -->
  <div class="flex items-center justify-between mt-8">
    <div class="flex items-center space-x-4">
      <%= link_to @content, 
          class: "inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 border h-10 px-4 py-2",
          style: "border-color: #E2E8F0; background-color: transparent; color: #1A202C; --tw-ring-color: #0052CC;" do %>
        <%= icon('eye', class: 'h-4 w-4 mr-2') %>
        미리보기
      <% end %>
      
      <%= link_to contents_path,
          class: "inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 border h-10 px-4 py-2",
          style: "border-color: #E2E8F0; background-color: transparent; color: #1A202C; --tw-ring-color: #0052CC;" do %>
        <%= icon('list', class: 'h-4 w-4 mr-2') %>
        목록으로
      <% end %>
    </div>
    
    <div class="flex items-center space-x-2">
      <p class="text-xs" style="color: #718096;">
        마지막 수정: <%= time_ago_in_words(@content.updated_at) %> ago
      </p>
    </div>
  </div>

  <!-- Danger Zone -->
  <div class="mt-8 rounded-lg border p-6" style="border-color: #E53E3E; background-color: #FFF5F5;">
    <h3 class="flex items-center text-sm font-medium mb-4" style="color: #E53E3E;">
      <%= icon('alert-triangle', class: 'h-4 w-4 mr-2') %>
      위험 구역
    </h3>
    <p class="text-sm mb-4" style="color: #718096;">
      이 콘텐츠를 삭제하면 복구할 수 없습니다. 신중히 결정해주세요.
    </p>
    <%= link_to @content,
        method: :delete,
        data: { 
          turbo_method: :delete,
          turbo_confirm: "정말로 '#{@content.title}'을(를) 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다." 
        },
        class: "inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 h-10 px-4 py-2",
        style: "background-color: #E53E3E; color: #FFFFFF; --tw-ring-color: #E53E3E;" do %>
      <%= icon('trash', class: 'h-4 w-4 mr-2') %>
      콘텐츠 삭제
    <% end %>
  </div>
</div>